# Prototypes

这个目录用于存放 TaihuCasino 的历史静态原型和导出版页面。

## 子目录

- `legacy-static/`
  原始静态首页/入口方案，包含旧的 `home*.html` 和 `index.html`
- `vercel-ver-static/`
  从 Vercel 风格方案导出的纯 `HTML + CSS + JS` 静态版本

## 约定

- 这里的内容保留用于参考、比对和迁移
- 正式功能开发不在这里继续进行
- 需要吸收原型内容时，应迁移到根目录的 Next.js 工程
