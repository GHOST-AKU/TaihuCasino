# 明天工作建议（基于当前仓库）

> 目标：把 `TaihuCasino` 从“可本地打开的单页 demo”推进到“可维护、可验证、可交付”的最小版本。

## 一、明天最值得做的 4 件事（按优先级）

1. **先把双 License 文件问题处理掉（`license` + `LICENSE`）**  
   现在仓库里同时存在 `license` 与 `LICENSE`，内容一致但大小写重复，容易在跨平台（尤其是 Windows/macOS）引发冲突与审查歧义。建议保留一个标准文件名（通常 `LICENSE`），另一个删除。  
   - 产出：单一许可证文件 + README 里明确引用。

2. **把 `baccarat.html` 与 `baccarat2.html` 做差异清点并决定“唯一主版本”**  
   目前仓库存在两个百家乐页面，后续修 bug 时会出现“改了 A 没改 B”的重复劳动。建议明天先做一次功能差异表，决定主文件（例如 `baccarat.html`），另一个标记为归档或删除。  
   - 产出：差异清单（功能/文案/UI）+ 主文件决策。

3. **为核心规则补一份最小化自测脚本（哪怕先手工）**  
   当前百家乐规则（如庄家补牌、赔率结算、庄抽 5% 佣金）都在前端脚本里，建议先列 8~10 条固定用例进行回归，至少覆盖：  
   - 庄/闲/和 三种主结果结算；
   - 庄赢佣金是否正确（`0.95:1`）；
   - 闲对子/庄对子（`11:1`）是否只看前两张牌；
   - 牌靴接近耗尽时是否自动重建。  
   - 产出：`TEST_CASES.md` 或 `tests/` 目录下的最小脚本。

4. **补一个“如何运行与验证”的 README（现在几乎没有项目说明）**  
   当前 README 主要是趣味 License 文本，缺少开发者真正需要的信息。建议新增：  
   - 项目结构（3 个主要页面分别做什么）；
   - 本地运行方式（直接打开/起本地静态服务）；
   - 基本验证流程（下注、发牌、重置、统计面板检查）；
   - 已知限制（纯前端、无后端持久化、随机数非可复现实验）。

## 二、建议日程（按 1 天可落地安排）

- **09:30–10:00：仓库清点**  
  确认重复文件、主入口页面、今天必须关闭的技术债。
- **10:00–12:00：结构收敛**  
  处理 License 重复 + 百家乐双文件主版本决策。
- **14:00–15:30：规则验证**  
  写最小测试清单并手工跑一轮，记录异常。
- **15:30–17:00：文档补齐**  
  更新 README，确保新人 5 分钟能跑起来并完成一次验证。
- **17:00–17:30：收尾**  
  提交变更并输出简短变更日志（做了什么、为什么、还有什么没做）。

## 三、明天可直接复制的验收清单

- [ ] 仓库只保留一个许可证文件名（建议 `LICENSE`）。
- [ ] 只保留一个主百家乐页面，另一个明确状态（删除/归档）。
- [ ] 至少有 8 条可复现的规则测试用例。
- [ ] README 包含“运行步骤 + 验证步骤 + 已知限制”。
- [ ] 完成一次从零打开页面到完成一轮下注结算的走查。

## 四、延期项（先不做）

- 先不做复杂前端框架迁移（React/Vue），避免一天内过度重构。
- 先不做后端化与账号系统，优先把规则正确性和可维护性打稳。
- 先不做视觉大改，先保证玩法正确、统计可信。
