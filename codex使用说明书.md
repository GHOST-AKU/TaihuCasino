# codex 使用说明书（Windows App 版）

> 适用对象：一直在用 Codex Cloud、刚开始使用 Windows Codex App 的开发者。  
> 目标：在 App 里稳定推进 `TaihuCasino` 项目。

---

## 0. 先了解你的项目是什么类型

`TaihuCasino` 当前是**静态网页项目**，核心文件是：

- `roulette.html`（欧式轮盘）
- `baccarat.html`（百家乐版本一）
- `baccarat2.html`（百家乐版本二）

这类项目最适合的 Codex 工作流是：

1. 明确需求（只做一个点）
2. 让 Codex 改单个 HTML 文件
3. 本地浏览器直接打开验证
4. git 提交

---

## 1. Windows Codex App 快速上手

## 1.1 打开项目

1. 启动 Codex App。
2. 点击 **Open Folder / 打开文件夹**。
3. 选择你的项目根目录：`TaihuCasino`。
4. 确认左侧文件树里能看到 `roulette.html`、`baccarat.html`、`baccarat2.html`。

## 1.2 建议先开一个任务分支

在 App 终端运行：

```bash
git checkout -b feat/mobile-roulette
```

> 每个需求一个分支，便于回滚和合并。

## 1.3 第一次给 Codex 发任务要用“结构化提示词”

推荐模板：

```text
目标：优化 roulette.html 的移动端布局。
约束：
1) 不改游戏规则逻辑，只改 HTML/CSS。
2) 保留现有配色和中文文案。
3) 修改范围仅限 roulette.html。
输出：
1) 修改摘要。
2) 风险点。
3) 手工验证步骤。
```

---

## 2. 在 App 里推进 TaihuCasino 的标准工作流

## Step A：先让 Codex 只出方案，不改代码

```text
先不要改代码。先给我 5 步执行计划 + 影响范围 + 风险。
```

你确认后再发：

```text
按上面的计划执行，直接修改文件。
```

## Step B：限制改动边界

每次任务都写清楚：

- 允许改哪些文件
- 禁止改哪些逻辑
- 验收标准是什么

示例：

```text
只允许修改 baccarat.html，禁止改结算逻辑（赔率和发牌规则）。
验收标准：桌面和手机宽度都不溢出，按钮不重叠。
```

## Step C：让 Codex 做“自检输出”

改完后要求它输出：

1. 改了哪些代码块
2. 为什么这么改
3. 可能副作用
4. 如何回滚

## Step D：你本地验收

静态页推荐两种方式：

- 直接双击 HTML 文件用浏览器打开。
- 或在终端跑轻量服务器（可选）：

```bash
python -m http.server 8000
```

然后访问 `http://localhost:8000/roulette.html`。

## Step E：提交改动

```bash
git add roulette.html
git commit -m "feat: 优化轮盘页面移动端布局"
```

---

## 3. 适合 TaihuCasino 的任务优先级（建议照这个顺序做）

1. **响应式布局**（最容易见效）
   - 手机端下注区、按钮区、统计区不重叠不溢出。
2. **交互反馈**
   - 输入非法金额提示、按钮禁用态、状态提示语清晰。
3. **代码可维护性**
   - 样式分组、命名统一、重复逻辑整理。
4. **玩法说明可读性**
   - Rules 弹窗结构更清晰。
5. **轻量性能优化**
   - 避免无意义 DOM 更新（如果有）。

---

## 4. 高质量提示词模板（可直接复制）

## 模板 1：只改 UI

```text
目标：优化移动端布局。
限制：仅可改 CSS/HTML，不得改 JS 结算逻辑。
文件范围：roulette.html。
交付：变更摘要 + 验收清单。
```

## 模板 2：先分析再改

```text
先做代码审查，不改代码。输出前10个可维护性问题并按优先级排序。
```

## 模板 3：最小改动策略

```text
使用“最小改动”原则解决问题：禁止大规模重构。
```

## 模板 4：带回滚指令

```text
改完请附带回滚命令（git restore / git checkout -- 文件）。
```

## 模板 5：拆分提交

```text
把需求拆成 2 次提交，每次给 commit message。
```

## 模板 6：加验收脚本

```text
给出我可以复制运行的手工验收步骤（含访问路径和检查点）。
```

---

## 5. 你最容易踩的坑（重点）

1. 一次给太多需求，导致改动失控。
2. 不写“禁止修改项”，导致逻辑被误改。
3. 不看 diff 就提交。
4. 不开分支直接在主线改。
5. 不做最小可验证测试。

---

## 6. 每次任务前后的检查清单（建议保存）

## 任务前

- [ ] 是否明确只做一个目标？
- [ ] 是否写明“禁止修改项”？
- [ ] 是否限制了文件范围？
- [ ] 是否定义了验收标准？

## 任务后

- [ ] 是否检查了 git diff？
- [ ] 是否本地打开页面验证桌面/手机宽度？
- [ ] 是否确认没有改坏赔率/结算逻辑？
- [ ] 是否完成 commit 且信息清晰？

---

## 7. 建议的 30 分钟推进节奏（非常实用）

- **0~5 分钟**：定义单一目标（例如：只改 `roulette.html` 移动端）
- **5~15 分钟**：Codex 修改
- **15~25 分钟**：你本地验证（布局、下注、结算展示）
- **25~30 分钟**：提交并记录下一步

---

## 8. 常用命令速查

```bash
# 查看状态
git status --short --branch

# 新建分支
git checkout -b feat/xxx

# 查看改动
git diff

# 提交
git add <file>
git commit -m "feat: xxx"

# 撤销单文件改动（未提交）
git restore <file>
```

---

## 9. 给未来自己的建议

你现在最重要的不是“会所有功能”，而是建立固定节奏：

- 每次只做一个目标；
- 每次都要求 Codex 先出计划；
- 每次都做最小验证；
- 每次都写清 commit。

做到这四点，你在 Windows Codex App 的效率会很快超过 Cloud 的“随手聊模式”。
